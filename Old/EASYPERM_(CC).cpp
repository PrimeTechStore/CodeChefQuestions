#include <bits/stdc++.h>
using namespace std;

#define ll long long

int main()
{
    ll t;
    cin >> t;
    while (t--)
    {
        int n = 0;
        cin >> n;

        for (int i = n; i > 0; i--)
            cout << i << " ";

        cout << endl;
    }

    return 0;
}